# 移除渠道访问白名单

- 用户要求：移除 AgentMux 的渠道白名单，由飞书管理机器人可用范围。
- 部署主机：`ecs_cn`。
- 部署时间：2026-09-06 14:25:33 +08:00。
- 版本：`0.1.9-no-channel-allowlist.1`，基于 `ac965c4bf6bdcbd479a2ae8aa4e23484ba7724f9` 和本次工作区修改。
- Linux amd64 二进制 SHA-256：`e8909bf2d0503d765e74d2c0c81b07d492fbb06e9ccb62df3854e47ee8d23722`。

## 行为

- 移除统一消息入口、远程任务入口、会议命令的渠道用户名单校验。
- 移除渠道编辑页的允许用户、管理员 Open ID 输入和扫码创建时的自动填充。
- 服务端保存渠道时丢弃历史 `allowed_user_ids`、`admin_user_ids`；历史值不再影响运行时权限。
- 任务停止、接管、排队控制、审批和群模式管理继续保留任务归属规则，管理身份来自飞书实际群主、群管理员。平台权限查询在任务锁之外执行，查询失败不授予管理权限。
- `reply_scope` 保留；两个线上渠道均为 `dm_and_mentions`。
- 会议恢复发现不再扫描配置名单，改为用户首次私聊或 @ 机器人时通过平台身份查询进行中会议；会议事件和显式会议查询继续可用。

## 验证

- `go test ./...` 通过。
- `go test -race ./core ./platform/feishu ./server` 通过。
- 前端 `npm test -- --reporter=dot`：25 个文件、107 项测试通过。
- 前端 `npm run build`、Linux amd64 嵌入网页构建通过。
- 回归覆盖：历史名单不能阻止新用户调用命令或排队；未 @ 的普通群消息不回复；实际 @、私聊和其他回复范围按配置工作；群管理操作依据平台身份，历史管理员名单无效。
- ECS CN 两个飞书渠道均为 `running`、`connected=true`。
- 数据库遗留名单字段数量为 0；与备份逐项比较，其他渠道配置完全一致。
- 线上渠道编辑器资源 SHA-256 与本地构建一致：`fbfec611996b66ee1a110c6153f268622e9217380e7ab38eb47a49798fc23be0`。
- 未主动发送真实群聊测试消息。

## 备份与部署过程

- 旧二进制和包含原始渠道配置的备份仅保存在 ECS CN：`/home/tiger/.agentmux/backups/remove-channel-access-lists-20260906T062516Z/`；配置备份权限为 `0600`。
- 部署前运行中、排队中的渠道任务数量为 0。
- 首次部署因检查窗口为 15 秒、与渠道健康轮询间隔相同而提前回滚。确认连接已正常建立后将检查窗口延长至 45 秒，再次部署并通过检查。
